2026-09-15T16:55:00Z	14	Wrote azure/valheim-peermeter-nft.sh (new table inet valheim_peermeter, peer_tx keyed by ip daddr on output/game_port, peer_rx keyed by ip saddr on input/game_port, dynamic+timeout sets mirroring valheim_meter conventions). Selftest passes locally and on VM. Deployed to /usr/local/sbin/ and installed on VM; valheim_meter counters confirmed unchanged (query_tx/rx identical across before/after nft list). Next: write JSON reader script, then relay-vs-direct capture mechanism.
2026-09-15T17:10:00Z	24	Wrote azure/valheim-peermeter-read.py (standalone JSON reader for valheim_peermeter, does not touch valheim-egress-probe.py). Selftest passes (5 fixture cases incl. malformed/one-sided/missing-set drop behavior). Deployed to VM /usr/local/sbin/; confirmed non-root invocation fails honestly (no root -> nft errors -> ok:false, no fabrication) and root invocation reads live empty peer set correctly. Next: relay-vs-direct capture mechanism (task 4), then non-disturbance verification writeup and PR.
2026-09-15T17:25:00Z	32	Wrote azure/valheim-relay-check.py (one-time/continuous capture watching valheim_peermeter's peer_rx for new addresses, classifies against built-in Valve/SDR CIDR ranges + PTR lookup, RFC1918 detection). Selftest passes. End-to-end validated live: started capture on VM (--timeout 60, background), sent synthetic UDP packet from operator's own machine (this session's Windows host, real network path) to VM public IP:2456, capture correctly detected the new peer_rx element, classified it likely_direct_client (PTR resolved to real ISP hostname, not a Valve range) and wrote JSON. Test artifacts (containing a real IP) deleted from VM /tmp afterward -- never committed. Next: task 5 non-disturbance verification (valheim_meter baseline compare, egress.service heartbeat check, valheim.service uptime check), then PR.
